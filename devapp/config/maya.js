module.exports = {
    log : {
        // reporting log level.
        // log level: "error" is most higher level.
        // "error" > "warn" > "debug" > "info" > "verbose" > "silly"
        level : "info"
    },

    view : {
        engine : "jade"
    }
};
